import { Date } from "../interfaces";

// export const getData: Date = async () => {
//   const url: string = "https://poloniex.com/public?command=returnTicker";

//   const response = await fetch(url);
//   const json = await response.json();
//   if (json.error) {
//     const error = new Error("Ошибка: " + json.error);
//     throw error;
//   } else {
//     return json;
//   }
// };
